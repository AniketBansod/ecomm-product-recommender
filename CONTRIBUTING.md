# Contributing Guidelines

- Do all development work on the `dev` branch.
- Use feature branches: `feature/<task-name>`.
- Open PRs from feature branch → dev.
- Do not push directly to main.
- Follow folder structure and naming conventions.
